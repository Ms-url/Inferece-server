传输内容	                单路码率	            10路总带宽	            千兆网卡占用
H.264 1080p 30fps	        4 Mbps	                40 Mbps	              3% ✅ 单线程够
RGB 640×480 30fps	        26 MB/s	                260 MB/s	          208% ❌ 单线程爆了
YOLO 输入 tensor 30fps	    12 MB/s	                120 MB/s	          96% ⚠️ 勉强够

read() 做的事就是 内核缓冲区 → 用户空间内存 的拷贝，瓶颈是内存带宽（~50 GB/s）

H.264 方案
epoll收流(4Mbps) ──▶ 线程池解码 ──▶ 帧队列 ──▶ 推理
  单线程够用          必须多线程      满了丢旧    动态batch

纯二进制方案
epoll收流(26MB/s) ──▶ 帧队列 ──▶ 推理
  ⚠️ 可能瓶颈         满了丢旧    动态batch
  
  ❌ 不需要解码线程池了 ✅
  ❌ 但收流可能要多线程 ⚠️

什么时候多线程 read 才有用？
场景	                多线程有用？	    原因
收视频流（几 Mbps）	    ❌ 没用	        网卡没跑满，单线程足够
收大文件（几百 MB/s）	✅ 有用	        网卡跑满了，需要多核分担系统调用
万路连接保活（心跳）	✅ 有用	        连接数巨大，单 epoll 循环不过来
SSD 读文件	            ✅ 有用	        磁盘 IOPS 高，多线程能跑满带宽

要开多线程的地方
操作	            在哪做	    需要多线程吗
epoll_wait等待事件	I/O 线程	❌ 不需要
read 收数据	        I/O 线程	❌ 不需要
解码心跳包	        I/O 线程	❌ 不需要（几微秒）
判定断连、清理资源	业务线程池	    ✅ 需要（避免阻塞 I/O 线程）
业务逻辑处理	    业务线程池	    ✅ 需要


坑	                          后果	                                    避免方法
每帧都推理一次	                GPU 空转，延迟 200ms+	                     必须做动态 batch
每路开一个 ORT Session	        显存爆炸，8路直接OOM	                     共享 Session + 队列
客户端传裸帧	                  1路就占 30Mbps+	                          必须 H.264 编码
同步处理 N 路	                  一路卡死全卡	                             每路独立队列，Worker 池隔离
结果队列无限堆积	              内存泄漏	                                设置队列 maxsize，旧结果丢弃


TCP 是一种 面向字节流的、面向连接的可靠传输协议，但由于它是流式的协议，在发送数据时并不保留消息的边界。因此，在实际的网络编程中，粘包与拆包问题就会出现，导致接收方无法正确区分一条完整的消息。

问题	          回答
什么是粘包？	  多个数据包粘在一起发送，接收方一次性读取多个消息。
什么是拆包？	  一个数据包被拆成多段，接收方需多次读取拼接。
为什么会出现？	TCP 是流协议，数据合并/分段发送，接收方不知边界。
如何解决？	    固定长度、添加分隔符、长度前缀、使用协议/框架


协议格式
[4字节: NALU长度][NALU数据][4字节: NALU长度][NALU数据]...


解码器数量限制？
丢弃最新元素无锁队列逻辑调整
客户端
