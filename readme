传输内容	                单路码率	            10路总带宽	            千兆网卡占用
H.264 1080p 30fps	        4 Mbps	                40 Mbps	              3% ✅ 单线程够
RGB 640×480 30fps	        26 MB/s	                260 MB/s	          208% ❌ 单线程爆了
YOLO 输入 tensor 30fps	    12 MB/s	                120 MB/s	          96% ⚠️ 勉强够

read() 做的事就是 内核缓冲区 → 用户空间内存 的拷贝，瓶颈是内存带宽（~50 GB/s）

H.264 方案
epoll收流(4Mbps) ──▶ 线程池解码 ──▶ 帧队列 ──▶ 推理
  单线程够用          必须多线程      满了丢旧    动态batch

纯二进制方案
epoll收流(26MB/s) ──▶ 帧队列 ──▶ 推理
  ⚠️ 可能瓶颈         满了丢旧    动态batch
  
  ❌ 不需要解码线程池了 ✅
  ❌ 但收流可能要多线程 ⚠️

什么时候多线程 read 才有用？
场景	                多线程有用？	    原因
收视频流（几 Mbps）	    ❌ 没用	        网卡没跑满，单线程足够
收大文件（几百 MB/s）	✅ 有用	        网卡跑满了，需要多核分担系统调用
万路连接保活（心跳）	✅ 有用	        连接数巨大，单 epoll 循环不过来
SSD 读文件	            ✅ 有用	        磁盘 IOPS 高，多线程能跑满带宽

要开多线程的地方
操作	            在哪做	    需要多线程吗
epoll_wait等待事件	I/O 线程	❌ 不需要
read 收数据	        I/O 线程	❌ 不需要
解码心跳包	        I/O 线程	❌ 不需要（几微秒）
判定断连、清理资源	业务线程池	    ✅ 需要（避免阻塞 I/O 线程）
业务逻辑处理	    业务线程池	    ✅ 需要


坑	                          后果	                                    避免方法
每帧都推理一次	                GPU 空转，延迟 200ms+	                     必须做动态 batch
每路开一个 ORT Session	        显存爆炸，8路直接OOM	                     共享 Session + 队列
客户端传裸帧	                  1路就占 30Mbps+	                          必须 H.264 编码
同步处理 N 路	                  一路卡死全卡	                             每路独立队列，Worker 池隔离
结果队列无限堆积	              内存泄漏	                                设置队列 maxsize，旧结果丢弃


TCP 是一种 面向字节流的、面向连接的可靠传输协议，但由于它是流式的协议，在发送数据时并不保留消息的边界。因此，在实际的网络编程中，粘包与拆包问题就会出现，导致接收方无法正确区分一条完整的消息。

问题	          回答
什么是粘包？	  多个数据包粘在一起发送，接收方一次性读取多个消息。
什么是拆包？	  一个数据包被拆成多段，接收方需多次读取拼接。
为什么会出现？	TCP 是流协议，数据合并/分段发送，接收方不知边界。
如何解决？	    固定长度、添加分隔符、长度前缀、使用协议/框架


协议格式
[Header][NALU数据]...


解码器数量限制？
丢弃最新元素无锁队列逻辑调整
客户端



项目名称：基于Reactor+epoll的高并发YOLO视频流推理服务端
项目概述
设计并实现了一个高并发、低延迟的视频流目标检测服务端，支持多路H.264编码视频的同时接入、实时解码与ONNX推理。系统采用Reactor并发模型与epoll I/O多路复用，实现同步非阻塞的网络通信，具备高吞吐、可扩展的服务能力。
技术架构与实现
•	网络层：基于Socket + TCP，采用Reactor + epoll边缘触发模式，高效管理数千并发连接，实现多路视频流的非阻塞接收与数据分发。
•	解码层：为每一路流分配独立解码器，结合线程池进行并行解码，保证同一连接（fd）最多只占用一个工作线程，避免锁竞争与资源浪费；在连接断开时，主动清理该fd对应的未执行任务，提升线程利用率。
•	推理层：采用生产者-消费者模型，解码线程池作为生产者将解码帧推入无锁推理等待队列；推理线程批量取出帧，动态组成Batch进行ONNX Runtime推理，并集成预处理、后处理与NMS，显著提升GPU利用率。
•	通信层：推理结果通过独立发送线程异步回传客户端，保证实时响应。
核心难点与优化
•	实现无锁队列，支持丢弃最旧元素策略，防止积压与内存溢出。
•	设计任务清理机制，在连接异常或断开时及时移除对应fd的等待任务，保证系统鲁棒性。
•	通过动态Batch推理与线程隔离策略，在保证实时性的同时最大化硬件资源利用率。

==========================精简========================
项目：基于Reactor+epoll的高并发YOLO视频流推理服务端
基于Reactor + epoll实现多路H.264视频流同步非阻塞接收，每路流绑定独立解码器并接入线程池并行解码。采用生产者-消费者模型，解码帧经无锁队列送入推理线程，动态组Batch进行ONNX Runtime推理（含预处理、后处理及NMS），结果异步回传客户端。优化同一fd仅占一线程、断连任务清理及队列丢弃最旧元素，保障系统高吞吐与稳定性。

========================精简+分点======================
项目：基于Reactor+epoll的高并发YOLO视频流推理服务端
•	基于Reactor + epoll边缘触发模式，实现多路H.264视频流的同步非阻塞网络接收与高并发连接管理。
•	为每路流分配独立解码器，线程池并行解码，确保同一fd仅占用一个工作线程，断连时主动清理未执行任务，避免资源浪费。
•	采用生产者-消费者模型，解码帧经无锁队列（支持丢弃最旧元素）送入推理线程，动态Batch进行ONNX Runtime推理，集成预处理、后处理及NMS。
•	推理结果通过独立发送线程异步回传客户端，保障实时响应与系统吞吐。
